version: 2

models:
  - name: bronze_date
    config:
      materialized: view
      schema: bronze
  - name: bronze_product
    config:
      materialized: view
      schema: bronze
  - name: bronze_sales
    columns:
      - name: sales_id
        data_tests:
          - not_null
          - unique
  - name: bronze_store
    columns:
      - name: store_sk
        data_tests:
          - not_null
          - unique
      - name: store_name
        data_tests:
          - accepted_values:
              arguments:
                values:
                  [
                    "MegaMart Manhattan",
                    "MegaMart Brooklyn",
                    "MegaMart Austin",
                    "MegaMart San Jose",
                    "MegaMart Toronto",
                  ]
